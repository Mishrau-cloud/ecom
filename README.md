# ecom

A simple Python utility for generating random passwords.

Import the helper from Python:

```python
from password_generator import generate_password

password = generate_password(length=16)
```